# Package-specific
TARGET := drone_artkp_kernel_Tracker
SOURCES := drone_artkp_kernel_Tracker.cpp

INCLUDES += -I$(ARTKP)/include/
LIBRARIES += -L$(ARTKP)/lib/ -lARToolKitPlus

# Common
OPTIONSDIR := ../../../common/
include $(OPTIONSDIR)Makefile.common